<Window x:Class="AutomationTestingSafety.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Смена пароля" Height="200" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Старый пароль:" Width="120"/>
            <PasswordBox x:Name="OldPasswordBox" Width="200"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Новый пароль:" Width="120"/>
            <PasswordBox x:Name="NewPasswordBox" Width="200"/>
        </StackPanel>
        <Button Grid.Row="2" Content="Сменить пароль" Width="150" Height="30" HorizontalAlignment="Center" Click="ChangePassword_Click"/>
    </Grid>
</Window>
